// Runtime configuration for the SmoothLingua demo. Override at deploy time by replacing
// this file with one that points to the actual API host. If unset, the page calls the API
// on the same origin (useful for local development where API and static files are served
// together).
window.SMOOTH_LINGUA_API_BASE = window.SMOOTH_LINGUA_API_BASE || '';
